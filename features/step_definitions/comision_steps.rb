When(/^sin lluvia$/) do
  pending
end

When(%r{^uso el endpoint a "/comisiones" con \{nombre_usuario: "([^"]*)"\}$}) do |_arg1, _arg2|
  pending
end

When(/^recibo \{id_pedido: (\d+), comision: (\d+)\}$/) do |_id_pedido, _comision|
  pending
end

When(/^con lluvia$/) do
  pending
end
